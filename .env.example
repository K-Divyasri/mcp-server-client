# Copy to .env and fill in to use --real mode (optional -- everything
# works offline with no key at all).
ANTHROPIC_API_KEY=

# Optional overrides (all have sane defaults, see mcp_kb/llm.py, weather.py, server.py)
# MCP_KB_MODEL=claude-haiku-4-5
# MCP_KB_DB_PATH=./data/notes.db
# MCP_KB_LIVE_WEATHER=0
# MCP_KB_HTTP_HOST=127.0.0.1
# MCP_KB_HTTP_PORT=8765
